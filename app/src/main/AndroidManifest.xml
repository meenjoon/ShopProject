<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shopproject"

    >

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShopProject"
        android:usesCleartextTraffic="true">


        <activity
            android:name=".Mapex"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBUftBu37tWYDmOJncD9urHl9ub4e3EqRY" />

        <activity
            android:name=".Buy_form"
            android:exported="false" />
        <activity
            android:name=".MultiImageAdapter"
            android:exported="false" />
        <activity
            android:name=".Camera_ex"
            android:exported="false" />
        <activity
            android:name=".Camera_example"
            android:exported="false" />
        <activity
            android:name=".Main_Activity"
            android:exported="false">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".FirstAuthActivity"
            android:exported="false" />
        <activity
            android:name=".Signup"
            android:exported="false" />
        <activity
            android:name=".Log_phone"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Log_before"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Camera"
            android:exported="false" />
        <activity
            android:name=".Sell_Form"
            android:exported="false" />
        <activity
            android:name=".Reset_Password"
            android:exported="false"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".UserAccount"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".Log_Activity"
            android:exported="true" /> <!-- <activity -->
        <!-- android:name=".Log_Activity_SharedPreferences" -->
        <!-- android:exported="true" > -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity
            android:name=".PreferenceManager"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".Buy"
            android:exported="false" />
        <activity
            android:name=".Product_Content"
            android:exported="false" /> <!-- 액티비티 추가시 <application> 안에 추가해줘야함 -->
        <activity android:name=".Manual" /> <!-- 액티비티 추가시 <application> 안에 추가해줘야함 -->
        <activity android:name=".Trade" /> <!-- 액티비티 추가시 <application> 안에 추가해줘야함 -->
        <activity android:name=".Log_JoinMembership" /> <!-- 액티비티 추가시 <application> 안에 추가해줘야함 -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".Camera_recyclerview"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.shopproject"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths3" />
        </provider>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false"/>

    </application>

</manifest>